import 'package:flutter/material.dart';

const Color moduleBackgroundColor = Color(0x00252626);
const double moduleShadowBlur = 3;
const int moduleShadowAlpha = 50;
const Offset moduleShadowOffset = Offset(0, 1);
